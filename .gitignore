node_modules/
.next/
dist/
build/
out/
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.local
.DS_Store
bun.lockb
# We use bun, not npm — don't accidentally commit npm's lockfile.
package-lock.json
# TypeScript incremental build cache (never useful in git).
tsconfig.tsbuildinfo
.turbo/
coverage/
.tauri/
src-tauri/target/
logs/
*.bak*
# Placeholder used by docker-compose when VAULT_PATH isn't set.
.no-vault/
